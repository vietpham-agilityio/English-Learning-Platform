/** Used when a category has no `currency` from the API (display + checkout hints). */
export const DEFAULT_CURRENCY = "USD";

/** Fallback amount (minor units) when `priceCents` is missing from the API. */
export const DEFAULT_PRICE_CENTS = 100;
